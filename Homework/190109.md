1. `abs`, `upper`, `find`, `join`, `insert`

2. 1

3. ```python
   def my_mul(*args):
       result=1
       for i in args:
           result*=i
       return result
   ```

   ```python
   def my_multi(a,b=1):
       return a*b
   ```


